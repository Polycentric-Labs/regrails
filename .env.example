# RegRails reads OPENROUTER_API_KEY from the environment at runtime.
# Allen's canonical location: C:\Users\allen\.secrets\openrouter.env (mounted, never copied here).
# Load with `set -a; source .env; set +a` or use direnv.
OPENROUTER_API_KEY=sk-or-v1-PASTE-YOUR-OWN-KEY-HERE
