/**
 * `/about` — About (PLACEHOLDER). Owned by task B3.10: thesis, role-mapping
 * (JD ↔ features), links (repo / PyPI / HF / Evidentia), the AI-assistance note.
 */
export default function About() {
  return (
    <section className="page" data-route="about">
      <h1 className="page-title">About</h1>
      <p className="page-sub">
        The deterministic-guardrail thesis and project links. (Route scaffold —
        content lands in B3.10.)
      </p>
    </section>
  );
}
