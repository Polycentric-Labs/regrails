/**
 * `/action` — GitHub Action (PLACEHOLDER). Owned by task B3.9: the composite
 * action + example workflow + "gate your AI pipeline on RegRails" copy.
 */
export default function Action() {
  return (
    <section className="page" data-route="action">
      <h1 className="page-title">Action</h1>
      <p className="page-sub">
        Gate your AI pipeline on RegRails. (Route scaffold — workflow lands in
        B3.9.)
      </p>
    </section>
  );
}
