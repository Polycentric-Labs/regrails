/**
 * `/benchmark` — Benchmark (PLACEHOLDER). Owned by task B3.4: held-out eval
 * from data/eval.json — per-model/scenario bars (Recharts), Wilson CIs,
 * Cohen's kappa, the Disagreements table.
 */
export default function Benchmark() {
  return (
    <section className="page" data-route="benchmark">
      <h1 className="page-title">Benchmark</h1>
      <p className="page-sub">
        Held-out evaluation across models. (Route scaffold — charts land in
        B3.4.)
      </p>
    </section>
  );
}
