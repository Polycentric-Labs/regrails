/**
 * `/coverage` — Coverage (PLACEHOLDER). Owned by task B3.3: the 31/37 matrix +
 * the 6 gap IDs with rationale from data/coverage.json.
 */
export default function Coverage() {
  return (
    <section className="page" data-route="coverage">
      <h1 className="page-title">Coverage</h1>
      <p className="page-sub">
        31 / 37 controls mapped; 6 documented gaps. (Route scaffold — matrix
        lands in B3.3.)
      </p>
    </section>
  );
}
