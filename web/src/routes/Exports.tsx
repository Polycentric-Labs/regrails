/**
 * `/exports` — Exports (PLACEHOLDER). Owned by task B3.7: OSCAL 1.1.2-shaped
 * catalog + SARIF 2.1.0 viewer/download from data/oscal.json + data/sarif.json,
 * with the honest claim-cap note.
 */
export default function Exports() {
  return (
    <section className="page" data-route="exports">
      <h1 className="page-title">Exports</h1>
      <p className="page-sub">
        OSCAL + SARIF interchange formats. (Route scaffold — viewers land in
        B3.7.)
      </p>
    </section>
  );
}
