/**
 * `/mcp` — MCP (PLACEHOLDER). Owned by task B3.8: the 3 tools
 * (consult_guardrail / list_rules / check_faithfulness) with signatures + a
 * recorded agent transcript. Documented, not a hosted live server.
 */
export default function Mcp() {
  return (
    <section className="page" data-route="mcp">
      <h1 className="page-title">MCP</h1>
      <p className="page-sub">
        Three tools for agentic clients. (Route scaffold — tool cards land in
        B3.8.)
      </p>
    </section>
  );
}
