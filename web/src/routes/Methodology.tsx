/**
 * `/methodology` — Methodology & Limitations (PLACEHOLDER). Owned by task B3.5:
 * renders data/methodology.json (scope boundary, faithfulness≠correctness,
 * not-legal-advice, the 6 coverage gaps).
 */
export default function Methodology() {
  return (
    <section className="page" data-route="methodology">
      <h1 className="page-title">Methodology</h1>
      <p className="page-sub">
        Scope, limitations, and honest framing. (Route scaffold — content lands
        in B3.5.)
      </p>
    </section>
  );
}
