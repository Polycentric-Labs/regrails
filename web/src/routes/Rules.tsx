/**
 * `/rules` — Rules browser (PLACEHOLDER). Owned by task B3.2: a filterable
 * table of the 37 rules from data/rules.json with verbatim CFR + section hash.
 */
export default function Rules() {
  return (
    <section className="page" data-route="rules">
      <h1 className="page-title">Rules</h1>
      <p className="page-sub">
        37 rules across FERPA + Title IV. (Route scaffold — table lands in B3.2.)
      </p>
    </section>
  );
}
